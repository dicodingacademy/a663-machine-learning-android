<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.MyCamera" parent="Theme.Material3.DayNight.NoActionBar">

        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MyCamera" parent="Base.Theme.MyCamera" />

    <style name="Theme.MyCamera.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.MyCamera.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.MyCamera.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>